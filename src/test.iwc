#include <time>
#include <math>
#include <test2>

double out2(int a,int b)
{
	int[] arrayTest = {{0,1,2,9}};
	out("Size of: "+arrayTest+" is "+arrayTest.length);
	printArrayContents(arrayTest);
	a = 0;
	string c = "Test";
	byte byteTest = 257;
	out(byteTest);
	out("arrayTest[0][3] is "+arrayTest[0][3]);
	out(a-b);
	boolean boolTest = false;
	out(!boolTest);
	if(!boolTest)
	{
		out(a+c+"!"-"est"+"1");
		out(a+c+"!"-"est"+"2");
		out(a+c+"!"-"est"+"3");
		out(a+c+"!"-"est"+"4");
		if(0)
		{
			out(a+c+"!"-"est"+"5");
		}
		out(a+c+"!"-"est"+"6");
	}
	double d = pow(pow(2,2),2);
	return d;
}

int main()
{
	test2();
	return out2(1,2);
}
